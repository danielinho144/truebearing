# Spoof model for trajectory simulation

#def spoof_jump(df, jump_time, jump_magnitude) -> pd.DataFrame
    # returns copy with spoofed_gps_x, spoofed_gps_y, columns
    # (first spoof mode - others like drift/replay come later as seperate functions)
